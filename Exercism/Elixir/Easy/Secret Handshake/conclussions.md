# Conclussions
1. You can use `Enum.join/2` if you want to join list of strings into one string
2. You can use `Enum.with_index()` func to e.g. iterate in Enum.reduce with index already in your enum

# Links
1. https://exercism.org/tracks/elixir/exercises/secret-handshake
